// tenants.js: implements the WebStarterTenant class

// load all required modules
const { Tenant, Tenants } = require("express-tenants");

// global list of tenants
const tenants = undefined;

// export fully loaded and initialized list of tenants
module.exports = tenants;
